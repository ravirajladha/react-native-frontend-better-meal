import React from 'react';

import {View} from 'react-native';

const Spcer15 = ({children}) => {
  return (
    <View
      style={{
        margin: 15,
      }}>
      {children}
    </View>
  );
};
export default Spcer15;
